# Later this will compare input fingerprint with DB fingerprints (cosine, hash, etc.)

def find_best_match(fp):
    """
    TO BE IMPLEMENTED:
    Return a fake match for now.
    """
    return {
        "song_id": 1,
        "score": 0.99
    }
